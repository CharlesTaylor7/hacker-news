import React from 'react';
import { NONAME } from 'dns';

const style = {
  margin: '10px',
  borderRadius: '50%',
  borderWidth: 0,
  background: 'grey',
  outline: 'none'
};
export default () => <button style={style}>+</button>;
